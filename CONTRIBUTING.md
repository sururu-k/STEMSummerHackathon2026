# 開発およびコントリビューション規約 (Contributing Guidelines)

本プロジェクト「会話クラッシャー晒し機」への貢献に関するガイドラインです。

---

## 1. ブランチ戦略 (Git-Flow / GitHub-Flow)

- `main` : プロダクション・発表用安定ブランチ
- `develop` : 統合開発ブランチ
- `feature/*` : 新機能開発用ブランチ
- `fix/*` : バグ修正用ブランチ
- `docs/*` : ドキュメント更新用ブランチ

---

## 2. コミットメッセージ規約 (Conventional Commits)

コミットメッセージは以下のプレフィックスを使用してください：

| Prefix | 説明 |
|---|---|
| `feat` | 新機能の追加 |
| `fix` | バグ修正 |
| `docs` | ドキュメントの追加・修正 |
| `arch` | アーキテクチャ設計・ADR |
| `refactor` | 機能追加を伴わないコード改善 |
| `perf` | パフォーマンス向上 |
| `test` | テストコードの追加・修正 |
| `chore` | ビルド設定、依存パッケージ更新 |
| `ci` | CI/CD 設定の変更 |

---

## 3. プルリクエスト (PR) ルール

1. 必ず PR テンプレートのチェックリストを満たしていることを確認してください。
2. CI (Marp ビルド検証・Lint) がパスしていることを確認してください。
3. デモ演出や音声処理の変更時は、リプレイモード (`sample-scenario.json`) での動作確認を行ってください。
